const config = {
  api: {
    development: "http://localhost:8080", // For local development (Maven server)
    // TODO: production url
    production: "https://your-production-api-url.com", // Production API endpoint
  },
};

export default config;
